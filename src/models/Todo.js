/**
 * Not using now
 */
export default class Todo {
  constructor(id, title, from, to, location, description, color, isCompleted) {
    this.id = id;
    this.title = title;
    this.from = from;
    this.to = to;
    this.location = location;
    this.description = description;
    this.color = color;
    this.isCompleted = isCompleted;
  }
}